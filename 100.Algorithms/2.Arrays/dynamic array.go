package main

/*// create an empty slice
var dynamicArray []int

// append elements to the slice
dynamicArray = append(dynamicArray, 1)
dynamicArray = append(dynamicArray, 2)
dynamicArray = append(dynamicArray, 3)

// access elements in the slice
fmt.Println(dynamicArray[0]) // prints 1
fmt.Println(dynamicArray[1]) // prints 2
fmt.Println(dynamicArray[2]) // prints 3

// update an element in the slice
dynamicArray[1] = 4

// delete an element from the slice
dynamicArray = append(dynamicArray[:1], dynamicArray[2:]...)
*/
